import type { Metadata } from "next"; export const metadata:Metadata={title:"About",description:"Read the proposed training approach for FORGE Training Club.",alternates:{canonical:"/about"},openGraph:{title:"About",description:"Read the proposed training approach for FORGE Training Club.",url:"/about"}}; export default function Layout({children}:{children:React.ReactNode}){return children}
