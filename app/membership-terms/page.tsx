import { LegalTemplate } from "@/components/legal/legal-template"; const topics=["Billing and renewals","Cancellation, freezing, and refunds","Classes and late arrivals","Gym rules, age restrictions, and guests","Belongings, health disclosures, and photography","Liability and disputes"]; export default function Page(){return <LegalTemplate title="Membership Terms" sections={topics.map(heading=>({heading,body:`Placeholder terms for ${heading.toLowerCase()}. FORGE must confirm its actual policy and obtain Lebanese legal review.`}))}/>}
