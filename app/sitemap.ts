import type { MetadataRoute } from "next"; import { gymConfig } from "@/config/gym"; import { classes } from "@/content/classes"; const paths=["","/classes","/schedule","/memberships","/trainers","/facilities","/trial-booking","/about","/faq","/contact","/privacy-policy","/terms-of-service","/cookie-policy","/membership-terms","/waiver-information","/accessibility"]; export default function sitemap():MetadataRoute.Sitemap{return [...paths,...classes.map(c=>"/classes/"+c.slug)].map(path=>({url:gymConfig.siteUrl+path,lastModified:new Date(),changeFrequency:path==="/schedule"?"weekly":"monthly",priority:path===""?1:.7}))}
