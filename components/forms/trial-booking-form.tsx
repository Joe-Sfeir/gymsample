"use client";
import { useState } from "react"; import { useForm } from "react-hook-form"; import { classes } from "@/content/classes"; import { trialBookingSchema,type TrialBookingInput } from "@/lib/validation/forms";
const fieldClass="mt-2 min-h-12 w-full border-2 border-border bg-background px-3";
export function TrialBookingForm(){const {register,handleSubmit,setError,reset,formState:{errors,isSubmitting}}=useForm<TrialBookingInput>(); const [status,setStatus]=useState<"idle"|"success"|"error">("idle"); const submit=async(data:TrialBookingInput)=>{setStatus("idle"); const checked=trialBookingSchema.safeParse(data); if(!checked.success){checked.error.issues.forEach(issue=>setError(issue.path[0] as keyof TrialBookingInput,{message:issue.message}));return} try{const response=await fetch("/api/trial-bookings",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(checked.data)}); if(!response.ok)throw new Error(); setStatus("success"); reset()}catch{setStatus("error")}}; const errorSummary=Object.values(errors).map(e=>e?.message).filter(Boolean); return <form className="max-w-3xl" noValidate onSubmit={handleSubmit(submit)}><div aria-live="assertive">{errorSummary.length>0&&<div className="mb-6 border-2 border-accent p-4" role="alert"><strong>Check the form:</strong><ul>{errorSummary.map((e,i)=><li key={i}>{e}</li>)}</ul></div>}{status==="success"&&<p className="mb-6 border-2 border-accent p-4" role="status">Trial request received in mock mode. A real delivery service is not connected.</p>}{status==="error"&&<div className="mb-6" role="alert"><p>Submission failed. Your details were not saved.</p><button className="underline" type="submit">Retry</button></div>}</div><div className="grid gap-5 sm:grid-cols-2"><Field label="Full name" error={errors.fullName?.message}><input className={fieldClass} autoComplete="name" {...register("fullName")}/></Field><Field label="Phone number" error={errors.phone?.message}><input className={fieldClass} autoComplete="tel" {...register("phone")}/></Field><Field label="Email" error={errors.email?.message}><input className={fieldClass} type="email" autoComplete="email" {...register("email")}/></Field><Field label="Preferred contact" error={errors.preferredContact?.message}><select className={fieldClass} {...register("preferredContact")} defaultValue=""><option value="" disabled>Select</option><option value="phone">Phone</option><option value="email">Email</option><option value="whatsapp">WhatsApp</option></select></Field><Field label="Experience level" error={errors.experienceLevel?.message}><select className={fieldClass} {...register("experienceLevel")} defaultValue=""><option value="" disabled>Select</option><option>Beginner</option><option>Intermediate</option><option>Advanced</option></select></Field><Field label="Preferred class" error={errors.preferredClass?.message}><select className={fieldClass} {...register("preferredClass")} defaultValue=""><option value="" disabled>Select</option>{classes.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></Field><Field label="Coaching type" error={errors.coachingType?.message}><select className={fieldClass} {...register("coachingType")} defaultValue="unsure"><option value="small-group">Small group</option><option value="one-to-one">One-to-one</option><option value="unsure">Not sure</option></select></Field><Field label="Preferred day" error={errors.preferredDay?.message}><input className={fieldClass} placeholder="e.g. Monday" {...register("preferredDay")}/></Field><Field label="Preferred time" error={errors.preferredTime?.message}><input className={fieldClass} placeholder="e.g. Evening" {...register("preferredTime")}/></Field></div><Field label="Training goal" error={errors.trainingGoal?.message}><textarea className={fieldClass+" min-h-28 p-3"} {...register("trainingGoal")}/></Field><Field label="Injury or limitation note (optional, sensitive)" error={errors.limitationNote?.message}><p className="text-sm text-muted">Shared only with the mock booking endpoint; never added to WhatsApp, analytics, or URLs.</p><textarea className={fieldClass+" min-h-28 p-3"} {...register("limitationNote")}/></Field><div className="absolute -left-[9999px]" aria-hidden="true"><label>Website<input tabIndex={-1} autoComplete="off" {...register("website")}/></label></div><label className="mt-5 flex gap-3"><input type="checkbox" {...register("consent")}/><span>I consent to FORGE using these details to respond to my request.</span></label>{errors.consent&&<p className="text-accent">{errors.consent.message}</p>}<label className="mt-4 flex gap-3"><input type="checkbox" {...register("whatsappOptIn")}/><span>Optional: contact me on WhatsApp.</span></label><button className="button button-accent mt-7" disabled={isSubmitting}>{isSubmitting?"Submitting…":"Request a trial"}</button></form>}
function Field({label,error,children}:{label:string;error?:string;children:React.ReactNode}){return <label className="mt-5 block font-bold">{label}{children}{error&&<span className="mt-1 block text-sm text-accent">{error}</span>}</label>}
