import { legalNotice } from "@/content/site";
export function LegalTemplate({ title, sections }: { title: string; sections: { heading: string; body: string }[] }) { return <article className="section"><div className="container max-w-3xl"><p className="eyebrow">Legal template</p><h1 className="mt-4 text-5xl font-black uppercase">{title}</h1><p className="placeholder-note my-8">{legalNotice}</p>{sections.map((section)=><section className="my-10" key={section.heading}><h2 className="text-2xl font-bold">{section.heading}</h2><p className="mt-3 text-muted">{section.body}</p></section>)}</div></article> }
