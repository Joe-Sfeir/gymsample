export interface RestClientOptions{baseUrl:string;headers?:Record<string,string>} export class RestClient{constructor(private options:RestClientOptions){} async request<T>(path:string,init?:RequestInit):Promise<T>{const response=await fetch(this.options.baseUrl+path,{...init,headers:{accept:"application/json",...this.options.headers,...init?.headers}});if(!response.ok)throw new ApiError(response.status,"API request failed");return response.json() as Promise<T>}} export class ApiError extends Error{constructor(public status:number,message:string){super(message);this.name="ApiError"}}
