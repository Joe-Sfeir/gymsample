import type { ScheduledSession,GymClass } from "@/types";
const esc=(value:string)=>value.replace(/\\/g,"\\\\").replace(/,/g,"\\,").replace(/;/g,"\\;").replace(/\n/g,"\\n"); const stamp=(date:string,time:string)=>date.replaceAll("-","")+"T"+time.replace(":","")+"00";
export function generateIcs(session:ScheduledSession,gymClass:GymClass){return ["BEGIN:VCALENDAR","VERSION:2.0","PRODID:-//FORGE Training Club//Schedule//EN","CALSCALE:GREGORIAN","BEGIN:VEVENT","UID:"+esc(session.id)+"@forge-training","DTSTAMP:"+new Date().toISOString().replace(/[-:]/g,"").replace(/\.\d{3}/,""),"DTSTART;TZID="+session.timeZone+":"+stamp(session.date,session.startTime),"DTEND;TZID="+session.timeZone+":"+stamp(session.date,session.endTime),"SUMMARY:"+esc(gymClass.name)+" — FORGE Training Club","DESCRIPTION:"+esc("Sample session. Confirm booking and schedule with FORGE."),"END:VEVENT","END:VCALENDAR"].join("\r\n")}
