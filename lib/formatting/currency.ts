export type BillingPeriod="monthly"|"quarterly"|"annual"; export function membershipPrice(plan:{monthlyPrice:number;quarterlyPrice:number;annualPrice:number},period:BillingPeriod){return period==="monthly"?plan.monthlyPrice:period==="quarterly"?plan.quarterlyPrice:plan.annualPrice} export function formatCurrency(value:number,currency:string,locale="en-LB"){return value===0?"[PRICE TO CONFIRM]":new Intl.NumberFormat(locale,{style:"currency",currency}).format(value)}
