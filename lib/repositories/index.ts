import type { Repositories } from "@/lib/repositories/interfaces"; import { mockRepositories } from "@/lib/repositories/mock"; export function getRepositories():Repositories{if(process.env.MOCK_DATA_MODE==="false")throw new Error("Live repositories are not configured. Set MOCK_DATA_MODE=true or provide a live adapter."); return mockRepositories} export type { Repositories } from "@/lib/repositories/interfaces";
