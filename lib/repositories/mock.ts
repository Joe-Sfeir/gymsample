import { classes,getClassBySlug,sampleSessions } from "@/content/classes"; import { trainers } from "@/content/trainers"; import { memberships } from "@/content/memberships"; import { facilities } from "@/content/site"; import type { Repositories } from "@/lib/repositories/interfaces";
export const mockRepositories:Repositories={classes:{list:async()=>classes,findBySlug:async slug=>getClassBySlug(slug)},schedule:{list:async()=>sampleSessions},trainers:{list:async()=>trainers},memberships:{list:async()=>memberships},facilities:{list:async()=>facilities},trialBookings:{create:async()=>({id:"mock-trial-request"})},contactInquiries:{create:async()=>({id:"mock-contact-request"})},capacity:{get:async sessionId=>{const s=sampleSessions.find(item=>item.id===sessionId);return s?{capacity:s.capacity,booked:s.bookedCount}:undefined}},waitlists:{join:async()=>({id:"mock-waitlist-entry"})}}
