import type { GymClass,ScheduledSession,Trainer } from "@/types"; export interface ScheduleFilters{day:string;classId:string;trainerId:string;intensity:string;level:string} export const emptyFilters:ScheduleFilters={day:"",classId:"",trainerId:"",intensity:"",level:""}; export function filterSessions(sessions:ScheduledSession[],classes:GymClass[],_trainers:Trainer[],filters:ScheduleFilters){return sessions.filter(session=>{const item=classes.find(c=>c.id===session.classId); return (!filters.day||session.date===filters.day)&&(!filters.classId||session.classId===filters.classId)&&(!filters.trainerId||session.trainerId===filters.trainerId)&&(!filters.intensity||item?.intensity===filters.intensity)&&(!filters.level||item?.experienceLevel===filters.level)})}
