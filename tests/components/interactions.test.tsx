import { describe,expect,it,vi,beforeEach } from "vitest"; import { render,screen } from "@testing-library/react"; import userEvent from "@testing-library/user-event"; import { ScheduleBrowser } from "@/components/schedule/schedule-browser"; import { PricingSelector } from "@/components/memberships/pricing-selector"; import { TrialBookingForm } from "@/components/forms/trial-booking-form"; import { ContactForm } from "@/components/forms/contact-form"; import { CookieConsent } from "@/components/consent/cookie-consent"; import { classes,sampleSessions } from "@/content/classes"; import { trainers } from "@/content/trainers"; import { memberships } from "@/content/memberships";
describe("interactive UI",()=>{beforeEach(()=>{localStorage.clear();vi.restoreAllMocks()});it("shows and resets the schedule empty state",async()=>{const user=userEvent.setup();render(<ScheduleBrowser sessions={sampleSessions} classes={classes} trainers={trainers}/>);await user.selectOptions(screen.getByLabelText("Class"),"mobility-recovery");await user.selectOptions(screen.getByLabelText("Trainer"),"coach-two");expect(screen.getByText("No sample sessions match.")).toBeInTheDocument();await user.click(screen.getByRole("button",{name:"Reset filters"}));expect(screen.queryByText("No sample sessions match.")).not.toBeInTheDocument()});it("changes membership billing selection",async()=>{const user=userEvent.setup();render(<PricingSelector plans={memberships}/>);await user.click(screen.getByRole("button",{name:"annual"}));expect(screen.getByRole("button",{name:"annual"})).toHaveAttribute("aria-pressed","true")});it("announces trial validation errors",async()=>{const user=userEvent.setup();render(<TrialBookingForm/>);await user.click(screen.getByRole("button",{name:"Request a trial"}));expect(await screen.findByText("Check the form:")).toBeInTheDocument()});it("submits contact form in mock mode",async()=>{vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true}));const user=userEvent.setup();render(<ContactForm/>);await user.type(screen.getByLabelText("Full name"),"Maya Haddad");await user.type(screen.getByLabelText("Phone or email"),"maya@example.com");await user.type(screen.getByLabelText("Message"),"I would like class information.");await user.click(screen.getByRole("checkbox",{name:/answer my inquiry/}));await user.click(screen.getByRole("button",{name:"Send inquiry"}));expect(await screen.findByText("Inquiry received in mock mode.")).toBeInTheDocument()});it("stores rejected optional cookies",async()=>{const user=userEvent.setup();render(<CookieConsent/>);expect(await screen.findByRole("dialog",{name:"Cookie choices"})).toBeInTheDocument();await user.click(screen.getByRole("button",{name:"Reject non-essential"}));expect(localStorage.getItem("forge-cookie-preferences")).toContain('"analytics":false')})});
