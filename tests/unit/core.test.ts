import { beforeEach,describe,expect,it } from "vitest"; import { classes,getClassBySlug,sampleSessions } from "@/content/classes"; import { trainers } from "@/content/trainers"; import { emptyFilters,filterSessions } from "@/lib/schedule/filter"; import { formatCurrency,membershipPrice } from "@/lib/formatting/currency"; import { trialBookingSchema,contactSchema } from "@/lib/validation/forms"; import { generalInquiryUrl,trialInquiryUrl } from "@/lib/whatsapp/urls"; import { generateIcs } from "@/lib/calendar/ics"; import { formatSessionDate,formatTime } from "@/lib/dates/format"; import { defaultPreferences,loadCookiePreferences,saveCookiePreferences } from "@/components/consent/cookie-consent";
describe("domain utilities",()=>{it("filters schedule and supports an empty state",()=>{expect(filterSessions(sampleSessions,classes,trainers,{...emptyFilters,classId:"engine"})).toHaveLength(1);expect(filterSessions(sampleSessions,classes,trainers,{...emptyFilters,classId:"missing"})).toEqual([])});it("looks up classes by slug",()=>{expect(getClassBySlug("forge-strength")?.name).toBe("FORGE Strength");expect(getClassBySlug("missing")).toBeUndefined()});it("selects and formats membership billing",()=>{const plan={monthlyPrice:10,quarterlyPrice:25,annualPrice:90};expect(membershipPrice(plan,"quarterly")).toBe(25);expect(formatCurrency(0,"USD")).toContain("CONFIRM")});it("validates trial and contact inputs",()=>{expect(trialBookingSchema.safeParse({}).success).toBe(false);expect(contactSchema.safeParse({fullName:"A",contact:"x",message:"no"}).success).toBe(false)});it("generates safe WhatsApp URLs without health details",()=>{expect(generalInquiryUrl()).toMatch(/^(#|https:\/\/wa\.me\/)/);expect(trialInquiryUrl({name:"Maya",preferredClass:"Engine"})).not.toContain("injury")});it("generates valid calendar content",()=>{const output=generateIcs(sampleSessions[0],classes[0]);expect(output).toContain("BEGIN:VCALENDAR");expect(output).toContain("TZID=Asia/Beirut");expect(output).toContain("END:VCALENDAR")});it("formats locale-aware dates and times",()=>{expect(formatSessionDate("2026-08-03")).toBeTruthy();expect(formatTime("18:00")).toBeTruthy()})});
describe("cookie storage",()=>{beforeEach(()=>localStorage.clear());it("persists preferences",()=>{saveCookiePreferences(defaultPreferences);expect(loadCookiePreferences()).toEqual(defaultPreferences)})});
